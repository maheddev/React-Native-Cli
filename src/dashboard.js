import {View, StyleSheet, Text} from 'react-native';
import {useRoute} from '@react-navigation/native';
const Dashboard = () => {
  return (
    <View>
      <Text>Hello! your Email is:</Text>
    </View>
  );
};
const styles = StyleSheet.create({});

export default Dashboard;
